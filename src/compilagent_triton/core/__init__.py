"""Backend-agnostic core: schemas, workload, search-space, runtime, observation glue.

Phase 1 introduces the package skeleton; subsequent phases populate the
content. Modules outside `core/` should program against this package and
`backends/base.py` only.
"""
